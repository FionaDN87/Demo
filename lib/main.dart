import 'package:demologinfionan/view/frontpage.dart';
import 'package:flutter/material.dart';

void main() => runApp(Demologinfionan());

class Demologinfionan extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return MaterialApp(
      home: FrontPage(),
    );
  }
}